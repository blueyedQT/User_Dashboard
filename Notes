Notes

Need to clean up .htaccess file, it is looking terrible currently :)
Need to change all text on landing page
Need to clean up code and comment
Would like to add images
Need to link to actual site
Set up DB
Change access info, so that it is not public info :)
Should make it so that the first person created in db is automatically an admin